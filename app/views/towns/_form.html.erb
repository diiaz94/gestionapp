<%= form_for(@town) do |f| %>
  <% if @town.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@town.errors.count, "error") %> prohibited this town from being saved:</h2>

      <ul>
      <% @town.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :state_id, "Seleccione estado" %><br>
    <%if @town.state%>
      <%= f.select :state_id, options_for_select(@states.pluck(:nombre,:id),@town.state.nombre)%>
    <%else%>
      <%= f.select :state_id, options_for_select(@states.pluck(:nombre,:id))%>
    <%end%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
