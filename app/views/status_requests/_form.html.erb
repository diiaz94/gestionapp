<%= form_for(@status_request) do |f| %>
  <% if @status_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_request.errors.count, "error") %> prohibited this status_request from being saved:</h2>

      <ul>
      <% @status_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
