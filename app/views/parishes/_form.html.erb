<%= form_for(@parish) do |f| %>
  <% if @parish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parish.errors.count, "error") %> prohibited this parish from being saved:</h2>

      <ul>
      <% @parish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :town_id, "Seleccione municipio" %><br>
    <%if @parish.town%>
      <%= f.select :town_id, options_for_select(@towns.pluck(:nombre,:id),@parish.town.nombre)%>
    <%else%>
      <%= f.select :town_id, options_for_select(@towns.pluck(:nombre,:id))%>
    <%end%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
